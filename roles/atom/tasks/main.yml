---
- name: Add atom repository
  apt_repository:
    repo: "ppa:webupd8team/atom"
  register: add_atom_repository

- name: Install atom
  apt:
    name: "atom"
    update_cache: yes
  when: add_atom_repository
