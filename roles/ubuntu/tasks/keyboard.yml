---
- name: Copy keyboard setting
  copy:
    src: "keyboard"
    dest: "/etc/default/keyboard"
    owner: root
    group: root
    mode: 0440
  become: yes
  register: kbs

- name: Enable keyboard setting
  command: dpkg-reconfigure -phigh console-setup
  become: yes
  when: kbs.changed
