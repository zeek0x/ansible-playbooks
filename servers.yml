---
- hosts: servers
  become: yes
  vars:
    __USERHOME__: "/home/{{ ansible_env.SUDO_USER }}"
  roles:
    - { role: ubuntu, when: "ansible_distribution == 'Ubuntu'" }
    - gcc
    - zip
    - wget
    - nmap
    - jq
    - mysql-client
    - sqlite3
    - thunderbird
    - virtualbox
    - vagrant
    - dropbox
    - software-properties-common

    # depneds on software-properties-common
    - atom
    - libreoffice
    - git

    # dpends on git
    - gibo
    - nvm
    - pyenv
    - rbenv
